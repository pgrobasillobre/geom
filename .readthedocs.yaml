version: 2

python:
  version: 3.10  # or any other version you prefer
  install:
    - requirements: docs/requirements.txt

build:
  os: ubuntu-20.04  # This should be the correct OS value
  tools:
    - sphinx

